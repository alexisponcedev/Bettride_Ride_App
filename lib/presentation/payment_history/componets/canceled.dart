import 'package:flutter/material.dart';

class PaymentCanceledScreen extends StatelessWidget {
  const PaymentCanceledScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
