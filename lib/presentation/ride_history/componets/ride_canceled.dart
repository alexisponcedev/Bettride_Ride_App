import 'package:flutter/material.dart';

class RideCanceledScreen extends StatelessWidget {
  const RideCanceledScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: const [],
    );
  }
}
