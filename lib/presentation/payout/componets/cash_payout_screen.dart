import 'package:flutter/material.dart';

class CashPayout extends StatelessWidget {
  const CashPayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
